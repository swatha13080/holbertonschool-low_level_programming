#include "main.h"

/**
 * _abs - Rertuns absolute value of int n
 * @n: the int to be checked
 *
 * Return: returns square root of the square of n
 */

int _abs(int n)
{
	return (n < 0 ? -1 * n : n);
}
